---
phase: 02-upload-ui
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/components/ShareStory.tsx
  - app/components/upload/UploadZone.tsx
  - app/components/upload/ProgressBar.tsx
  - app/components/upload/FileListRow.tsx
  - app/components/upload/ErrorBanner.tsx
  - app/page.tsx
autonomous: true
requirements: [UPLD-01, UPLD-02, UPLD-03, UPLD-04, UPLD-05]

must_haves:
  truths:
    - "User can tap the drop zone to open a file picker on iOS Safari"
    - "User can drag-and-drop a file onto the drop zone on desktop"
    - "Progress bar fills from 0 to 100% during upload with real-time XHR events"
    - "Error banner appears with message and retry link when upload fails"
    - "Selecting a non-video/non-audio file shows a type validation error before upload starts"
    - "File list shows each selected file with name, size, and remove button"
    - "Submit button is disabled and reads UPLOADING... during upload"
  artifacts:
    - path: "app/components/ShareStory.tsx"
      provides: "Orchestrator component wired to useFileUpload hook"
      exports: ["default"]
    - path: "app/components/upload/UploadZone.tsx"
      provides: "Drag-drop + click-to-browse with file type validation"
      exports: ["default"]
    - path: "app/components/upload/ProgressBar.tsx"
      provides: "ARIA progressbar driven by upload progress state"
      exports: ["default"]
    - path: "app/components/upload/FileListRow.tsx"
      provides: "Per-file row with status and remove action"
      exports: ["default"]
    - path: "app/components/upload/ErrorBanner.tsx"
      provides: "Error display with retry action, role=alert"
      exports: ["default"]
    - path: "app/page.tsx"
      provides: "Imports ShareStory from components instead of inline definition"
  key_links:
    - from: "app/components/ShareStory.tsx"
      to: "app/hooks/useFileUpload.ts"
      via: "import { useFileUpload }"
      pattern: "useFileUpload"
    - from: "app/components/upload/UploadZone.tsx"
      to: "app/utils/fileValidation.ts"
      via: "import { isAcceptedFileType }"
      pattern: "isAcceptedFileType"
    - from: "app/components/ShareStory.tsx"
      to: "app/components/upload/UploadZone.tsx"
      via: "JSX composition"
      pattern: "<UploadZone"
    - from: "app/page.tsx"
      to: "app/components/ShareStory.tsx"
      via: "import default"
      pattern: "import ShareStory"
---

<objective>
Extract the ShareStory component from page.tsx into its own file, build the upload sub-components (UploadZone, ProgressBar, FileListRow, ErrorBanner), and wire everything to the useFileUpload hook from Plan 01.

Purpose: Replace the fake `setSubmitted(true)` prototype behavior with a real XHR upload pipeline that provides progress feedback and error handling.
Output: A fully wired ShareStory component with real upload behavior, ready for device testing.
</objective>

<execution_context>
@/Users/chrisaumen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisaumen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-upload-ui/02-RESEARCH.md
@.planning/phases/02-upload-ui/02-UI-SPEC.md
@.planning/phases/02-upload-ui/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (app/hooks/useFileUpload.ts) -->
type UploadStatus = 'idle' | 'uploading' | 'complete' | 'error';
export function useFileUpload(): {
  upload: (file: File) => Promise<string | null>;
  progress: number;
  status: UploadStatus;
  error: string | null;
  reset: () => void;
}

<!-- From Plan 01 (app/utils/fileValidation.ts) -->
export function isAcceptedFileType(file: Pick<File, 'type'>): boolean;

<!-- COLORS constant from app/page.tsx lines 4-15 -->
const COLORS = {
  black: "#111111", darkGray: "#1A1A1A", midGray: "#2A2A2A",
  cardGray: "#1F1F1F", warmGray: "#9CA3AF", lightGray: "#E8E8E8",
  offWhite: "#F5F5F5", white: "#FFFFFF", red: "#B91C1C", darkRed: "#7F1D1D",
};

<!-- FadeIn and RedBar are defined in page.tsx — ShareStory.tsx must receive them as props or import COLORS directly -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build upload sub-components</name>
  <files>app/components/upload/UploadZone.tsx, app/components/upload/ProgressBar.tsx, app/components/upload/FileListRow.tsx, app/components/upload/ErrorBanner.tsx</files>
  <read_first>
    - app/page.tsx (lines 263-446 — current ShareStory visual prototype, exact inline styles, COLORS usage)
    - app/utils/fileValidation.ts (isAcceptedFileType import for UploadZone)
    - .planning/phases/02-upload-ui/02-UI-SPEC.md (Component Inventory section — exact states, colors, spacing, copy, accessibility attributes)
    - .planning/phases/02-upload-ui/02-RESEARCH.md (Pattern 2: iOS-safe file input with useRef, Pattern 3: MIME validation before upload)
  </read_first>
  <action>
Create `app/components/upload/` directory with four sub-components. Every component must have `"use client";` directive. Each component uses inline styles matching the COLORS constant values defined below (copy COLORS into each file or define a shared constant file — but since the project convention is inline styles, hardcode hex values directly).

**COLORS reference (use these exact hex values):**
- darkGray: `#1A1A1A`, midGray: `#2A2A2A`, cardGray: `#1F1F1F`
- warmGray: `#9CA3AF`, lightGray: `#E8E8E8`, white: `#FFFFFF`
- red: `#B91C1C`, darkRed: `#7F1D1D`

---

**UploadZone.tsx** — Props: `{ onFilesSelected: (files: File[]) => void, disabled: boolean }`

- Uses `useRef<HTMLInputElement>(null)` for the hidden file input (NOT document.getElementById)
- Outer div: `role="button"`, `tabIndex={0}`, `onClick` calls `fileInputRef.current?.click()`, `onKeyDown` for Enter/Space
- `onDragOver`: `e.preventDefault()`, set dragOver=true
- `onDragLeave`: set dragOver=false
- `onDrop`: `e.preventDefault()`, set dragOver=false, filter files through `isAcceptedFileType`, call `onFilesSelected` with valid files, if any rejected files exist call a local `setTypeError` state to show ErrorBanner inline
- Hidden input: `type="file"`, `accept="video/*,audio/*"`, `multiple`, `ref={fileInputRef}`, `style={{ display: 'none' }}`
- onChange handler: filter through `isAcceptedFileType`, same rejection logic as drop
- Idle styles: `border: '2px dashed #9CA3AF40'`, `background: '#1F1F1F'`, `padding: '48px 24px'`, `textAlign: 'center'`, `cursor: 'pointer'`, `transition: 'all 0.3s'`
- Drag-active styles: `border: '2px dashed #B91C1C'`, `background: '#B91C1C08'`
- Disabled styles: `pointerEvents: 'none'`, `opacity: 0.5`
- Content: emoji `🎬` at 36px mb 12px opacity 0.6, heading "Drop your video or voice memo here" (Source Sans 3, 16px, weight 600, white), subtext "or tap to browse — accepts video and audio files" (Source Sans 3, 14px, warmGray `#9CA3AF`). Note: subtext says "tap" not "click" per UI-SPEC copywriting contract.
- If `typeError` state is set, render ErrorBanner below the drop zone content with message "That file type isn't supported. Please select a video or audio file."
- Do NOT use `capture` attribute on the input (would force iOS camera, blocking access to existing files)

---

**ProgressBar.tsx** — Props: `{ progress: number, status: 'idle' | 'uploading' | 'complete' | 'error' }`

- Container div: `role="progressbar"`, `aria-valuenow={progress}`, `aria-valuemin={0}`, `aria-valuemax={100}`
- Hidden when status is 'idle' and progress is 0: `style={{ display: (status === 'idle' && progress === 0) ? 'none' : 'block' }}`
- Track: full width, height 4px, background `#2A2A2A`, overflow hidden
- Fill: `width: \`${progress}%\``, height 4px, background `#B91C1C`, transition `width 0.1s linear`
- Label below: `{progress}% uploaded` — Source Sans 3, 14px, weight 400, color `#9CA3AF`, text-align right, margin-top 4px

---

**FileListRow.tsx** — Props: `{ file: File, status: 'pending' | 'uploading' | 'done' | 'failed', onRemove: () => void }`

- Container: `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'space-between'`, `background: '#2A2A2A'`, `padding: '8px 16px'`, `borderLeft: '3px solid #B91C1C'`
- Left side: file name (Source Sans 3, 14px, color `#E8E8E8`), file size in MB `(file.size / 1024 / 1024).toFixed(1) MB` (Source Sans 3, 14px, color `#9CA3AF`, margin-left 8px)
- Right side: status indicator
  - `pending`: no indicator
  - `uploading`: text "Uploading..." (Source Sans 3, 14px, color `#9CA3AF`)
  - `done`: checkmark text "Done" (Source Sans 3, 14px, color `#9CA3AF`) — use text, not a CSS shape
  - `failed`: text "Failed" (Source Sans 3, 14px, color `#B91C1C`, weight 600) + " " + button "Try again" (color `#B91C1C`, weight 600, background none, border none, cursor pointer)
- Remove button (rightmost): text content `\u00d7` (multiplication sign), 14px, color `#9CA3AF`, background none, border none, cursor pointer, margin-left 12px. Hidden when status is 'uploading' or 'done'.

---

**ErrorBanner.tsx** — Props: `{ message: string, onRetry?: () => void }`

- Container: `role="alert"`, `background: '#7F1D1D20'`, `border: '1px solid #B91C1C'`, `padding: '12px 16px'`
- Animation on mount: `opacity 0 -> 1`, `translateY 8px -> 0`, duration 0.25s ease. Implement via inline style + useEffect setting opacity/transform after mount, or a CSS @keyframes if simpler.
- Message text: Source Sans 3, 14px, weight 600, color `#B91C1C`
- Retry button (only rendered if onRetry provided): text "Try again", Source Sans 3, 14px, weight 600, color `#B91C1C`, background none, border none, cursor pointer, margin-left 4px
- Dismissal: ErrorBanner is controlled by parent — parent sets message to null/empty to dismiss
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/components/upload/UploadZone.tsx contains `useRef<HTMLInputElement>(null)` (not document.getElementById)
    - app/components/upload/UploadZone.tsx contains `accept="video/*,audio/*"` on the input element
    - app/components/upload/UploadZone.tsx contains `role="button"` and `tabIndex={0}`
    - app/components/upload/UploadZone.tsx contains `isAcceptedFileType` import from `@/app/utils/fileValidation`
    - app/components/upload/UploadZone.tsx contains `e.preventDefault()` in onDragOver handler
    - app/components/upload/UploadZone.tsx contains the string "or tap to browse" (not "or click to browse")
    - app/components/upload/UploadZone.tsx does NOT contain `document.getElementById`
    - app/components/upload/UploadZone.tsx does NOT contain `capture=`
    - app/components/upload/ProgressBar.tsx contains `role="progressbar"` and `aria-valuenow`
    - app/components/upload/ProgressBar.tsx contains `width: 0.1s linear` transition
    - app/components/upload/ProgressBar.tsx contains the string `% uploaded`
    - app/components/upload/FileListRow.tsx contains `borderLeft: '3px solid #B91C1C'`
    - app/components/upload/ErrorBanner.tsx contains `role="alert"`
    - app/components/upload/ErrorBanner.tsx contains `#7F1D1D20` background
    - app/components/upload/ErrorBanner.tsx contains "Try again" button text
    - All four files contain `"use client"` directive
    - `npx next build` succeeds (exit 0)
  </acceptance_criteria>
  <done>Four upload sub-components exist with correct styles, accessibility attributes, and props interfaces matching the UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2: Extract ShareStory and wire to upload hook</name>
  <files>app/components/ShareStory.tsx, app/page.tsx</files>
  <read_first>
    - app/page.tsx (full file — need to understand ShareStory inline definition at lines 263-446 AND how page.tsx uses it, plus COLORS constant, FadeIn, RedBar definitions)
    - app/hooks/useFileUpload.ts (hook interface from Plan 01)
    - app/utils/fileValidation.ts (isAcceptedFileType from Plan 01)
    - app/components/upload/UploadZone.tsx (created in Task 1)
    - app/components/upload/ProgressBar.tsx (created in Task 1)
    - app/components/upload/FileListRow.tsx (created in Task 1)
    - app/components/upload/ErrorBanner.tsx (created in Task 1)
    - .planning/phases/02-upload-ui/02-UI-SPEC.md (Interaction States, Copywriting Contract, Component Inventory)
    - .planning/phases/02-upload-ui/02-RESEARCH.md (Pattern 1: XHR state machine, open question 2: submit button triggers upload only, open question 3: store fileKey in state)
  </read_first>
  <action>
**Step 1: Create `app/components/ShareStory.tsx`**

This is a `"use client"` component that replaces the inline ShareStory function from page.tsx. It must:

1. **Copy the COLORS constant** from page.tsx (lines 4-15) into this file, or import from a shared location. Since no shared COLORS file exists, duplicate the constant in this file (same pattern as page.tsx).

2. **Copy FadeIn and RedBar** from page.tsx into this file (or create local versions). FadeIn uses IntersectionObserver — copy the `useInView` hook and `FadeIn` component. RedBar is a simple styled div. These are needed because page.tsx defines them as local functions.

3. **State management:**
   - `const [files, setFiles] = useState<File[]>([])` — selected files queue
   - `const [fileKey, setFileKey] = useState<string | null>(null)` — stored for Phase 3
   - `const [name, setName] = useState("")` — user name field (keep for confirmation screen personalization)
   - `const [relation, setRelation] = useState("")` — connection field (keep for Phase 3)
   - `const [submitted, setSubmitted] = useState(false)` — post-upload confirmation transition
   - `const [fileStatuses, setFileStatuses] = useState<Record<number, 'pending' | 'uploading' | 'done' | 'failed'>>({})` — per-file upload status
   - `const { upload, progress, status, error, reset } = useFileUpload()` — from Plan 01 hook

4. **File selection handler** (called by UploadZone's `onFilesSelected`):
   - Append new files to `files` state
   - Initialize their status as `'pending'` in `fileStatuses`

5. **Submit button behavior (handleSubmit):**
   - Filter files with status `'pending'` or `'failed'`
   - Upload sequentially (one at a time, not Promise.all) using a `for...of` loop:
     ```
     for (const [index, file] of filesToUpload) {
       setFileStatuses(prev => ({ ...prev, [index]: 'uploading' }));
       const key = await upload(file);
       if (key) {
         setFileStatuses(prev => ({ ...prev, [index]: 'done' }));
         setFileKey(key); // store last successful key for Phase 3
       } else {
         setFileStatuses(prev => ({ ...prev, [index]: 'failed' }));
       }
     }
     ```
   - After all files processed: if all done, `setSubmitted(true)` to show confirmation
   - Button disabled when `status === 'uploading'`
   - Button text: `status === 'uploading' ? 'UPLOADING...' : 'SUBMIT YOUR STORY'`

6. **Remove file handler:** Remove file from `files` array and corresponding `fileStatuses` entry. Only allowed when file status is `'pending'` or `'failed'`.

7. **Layout structure** (preserves exact visual design from page.tsx prototype):
   ```
   <section id="story" style={{ background: '#1A1A1A', padding: '100px 24px', position: 'relative', overflow: 'hidden' }}>
     {/* noise texture overlay — copy from page.tsx */}
     <div style={{ maxWidth: 700, margin: '0 auto', position: 'relative', zIndex: 1 }}>
       <FadeIn>
         <p style={{ ... }}>SHARE YOUR STORY</p>
         <RedBar />
       </FadeIn>
       <FadeIn delay={0.15}>
         <h2>Were you coached by Mouse?</h2>
         <p>We're collecting stories...</p>
       </FadeIn>
       {!submitted ? (
         <FadeIn delay={0.3}>
           {/* Name + relation inputs (2-column grid, same styles as prototype) */}
           <UploadZone onFilesSelected={handleFileSelection} disabled={status === 'uploading'} />
           {/* File list */}
           {files.map((f, i) => (
             <FileListRow
               key={i}
               file={f}
               status={fileStatuses[i] || 'pending'}
               onRemove={() => handleRemoveFile(i)}
             />
           ))}
           <ProgressBar progress={progress} status={status} />
           {error && <ErrorBanner message={error} onRetry={() => { reset(); /* re-trigger upload */ }} />}
           <button onClick={handleSubmit} disabled={status === 'uploading' || files.length === 0} style={{ ... }}>
             {status === 'uploading' ? 'UPLOADING...' : 'SUBMIT YOUR STORY'}
           </button>
           <p style={{ ... }}>By uploading, you grant permission...</p>
         </FadeIn>
       ) : (
         <FadeIn>
           {/* Confirmation card — same as prototype */}
         </FadeIn>
       )}
     </div>
   </section>
   ```

8. **Button disabled styling:** When `disabled`, add `opacity: 0.4`, `cursor: 'not-allowed'`. When not disabled, keep existing hover behavior (background `#7F1D1D` on mouseEnter, `#B91C1C` on mouseLeave).

9. **All inline styles must use the exact hex values from the COLORS constant.** Do not use Tailwind classes (project convention for this component is inline styles).

10. **Copy the permission disclaimer text exactly:** "By uploading, you grant permission for your story to be considered as research material for the documentary. We may reach out to schedule a formal interview."

**Step 2: Update `app/page.tsx`**

1. Remove the inline `ShareStory` function definition (lines 263-446).
2. Add import at top: `import ShareStory from "@/app/components/ShareStory";`
3. Keep `<ShareStory />` usage in the JSX exactly where it was.
4. Keep COLORS, useInView, FadeIn, RedBar, and all other components in page.tsx (they are still used by other sections). ShareStory.tsx gets its own copies.
5. Verify no other component in page.tsx depends on the removed ShareStory function.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - app/components/ShareStory.tsx exists and contains `"use client"` directive
    - app/components/ShareStory.tsx contains `import { useFileUpload }` from `@/app/hooks/useFileUpload`
    - app/components/ShareStory.tsx contains `import UploadZone` from `@/app/components/upload/UploadZone`
    - app/components/ShareStory.tsx contains `import ProgressBar` from `@/app/components/upload/ProgressBar`
    - app/components/ShareStory.tsx contains `import FileListRow` from `@/app/components/upload/FileListRow`
    - app/components/ShareStory.tsx contains `import ErrorBanner` from `@/app/components/upload/ErrorBanner`
    - app/components/ShareStory.tsx contains `useState<string | null>(null)` for fileKey storage
    - app/components/ShareStory.tsx contains `UPLOADING...` text for button loading state
    - app/components/ShareStory.tsx contains `opacity: 0.4` for disabled button state
    - app/components/ShareStory.tsx does NOT contain `document.getElementById`
    - app/page.tsx contains `import ShareStory from "@/app/components/ShareStory"`
    - app/page.tsx does NOT contain `function ShareStory()` (inline definition removed)
    - `npx next build` succeeds (exit 0)
    - `npm test` exits 0 (existing Phase 1 tests still pass)
  </acceptance_criteria>
  <done>ShareStory extracted from page.tsx, wired to useFileUpload hook, sub-components composed, build succeeds, existing tests pass</done>
</task>

</tasks>

<verification>
Build succeeds with no errors:
```bash
npx next build
```

All tests pass (Phase 1 + Phase 2):
```bash
npm test
```

ShareStory component is properly imported in page.tsx:
```bash
grep -n "import ShareStory" app/page.tsx
```

No document.getElementById in any new component:
```bash
grep -r "document.getElementById" app/components/ && echo "FAIL: found document.getElementById" || echo "PASS: no document.getElementById"
```
</verification>

<success_criteria>
- Four sub-components (UploadZone, ProgressBar, FileListRow, ErrorBanner) exist with correct props, styles, and accessibility
- ShareStory extracted to own file, wired to useFileUpload hook
- page.tsx imports ShareStory, inline definition removed
- Upload button triggers sequential XHR upload of selected files
- Progress bar updates during upload
- Error banner shows on failure with retry
- File type validation rejects non-video/non-audio before upload
- Build succeeds, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-upload-ui/02-02-SUMMARY.md`
</output>
