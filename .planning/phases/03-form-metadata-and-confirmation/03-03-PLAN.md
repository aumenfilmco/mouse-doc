---
phase: 03-form-metadata-and-confirmation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements: [FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, FORM-06, META-01, META-02, CONF-01, CONF-02]

must_haves:
  truths:
    - "File submission creates Airtable record with all fields populated"
    - "Text-only submission creates Airtable record with story text and no file key"
    - "Submitting without file or text story is blocked"
    - "Confirmation screen shows submitter name after successful submit"
    - "Chris can see all submissions in Airtable"
  artifacts: []
  key_links: []
---

<objective>
Deploy a preview and manually verify the complete submission flow across all paths: file upload, text-only, validation errors, metadata failure retry, and confirmation screen.

Purpose: End-to-end verification that cannot be automated — requires real Airtable credentials and visual confirmation.
Output: Verified working submission pipeline ready for production.
</objective>

<execution_context>
@/Users/chrisaumen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chrisaumen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-form-metadata-and-confirmation/03-01-SUMMARY.md
@.planning/phases/03-form-metadata-and-confirmation/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and full test suite before verification</name>
  <files></files>
  <read_first>
    - app/components/ShareStory.tsx (verify final state)
    - app/api/submit/metadata/route.ts (verify final state)
    - app/utils/validateSubmission.ts (verify final state)
  </read_first>
  <action>
1. Run `npx next build` — must succeed with no errors.
2. Run `npx jest` — full suite must be green.
3. If either fails, diagnose and fix before proceeding to checkpoint.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10 && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits with code 0
    - `npx jest` shows all tests passing with no failures
  </acceptance_criteria>
  <done>Build and tests green. Ready for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual end-to-end verification of submission flow</name>
  <files></files>
  <read_first>
    - app/components/ShareStory.tsx (final component state)
  </read_first>
  <action>
Present the following manual verification steps to the user and wait for approval.

Pre-requisites: Airtable env vars must be set in .env.local (AIRTABLE_PAT, AIRTABLE_BASE_ID, AIRTABLE_TABLE_ID). Airtable table must have columns: Name, Connection, Email, Phone, StoryText, FileKey, SubmittedAt.

Start dev server: npm run dev

Test 1 — Validation errors (FORM-01, FORM-02, FORM-06):
1. Navigate to http://localhost:3000/#story
2. Leave all fields empty, click "SUBMIT YOUR STORY"
3. Expect: error "Your name is required." appears below name field
4. Type a name, click submit again
5. Expect: error "Your connection to Coach McCollum is required." appears below relation field
6. Type a relation, click submit again (no file, no text)
7. Expect: error "Please add a file or type your story — we need at least one."

Test 2 — Email validation (FORM-03):
1. Type "bad" in email field, tab out
2. Expect: "Please enter a valid email address." below email field
3. Change to "good@example.com", tab out
4. Expect: error disappears

Test 3 — Text-only submission (FORM-05, META-01, CONF-01, CONF-02):
1. Fill in name + relation, type a story in textarea, no file
2. Click submit
3. Expect: button shows "SAVING...", then confirmation card with "Thank you, {name}."
4. Open Airtable — verify a record exists with Name, Connection, StoryText populated, FileKey empty

Test 4 — File submission (META-01, META-02, CONF-01):
1. Refresh page
2. Fill in name + relation + email + phone
3. Upload a small audio/video file
4. Click submit
5. Expect: button shows "UPLOADING...", progress bar fills, then "SAVING...", then confirmation
6. Open Airtable — verify record with all fields including FileKey (a submissions/YYYY-MM/... path)

Test 5 — Mobile grid (responsive):
1. Resize browser to less than 640px width
2. Verify name/relation stack vertically, email/phone stack vertically
  </action>
  <verify>User confirms all 5 test scenarios pass by typing "approved"</verify>
  <acceptance_criteria>
    - User confirms validation errors appear correctly for empty name, empty relation, and no-file-no-text
    - User confirms email format validation works on blur
    - User confirms text-only submission creates Airtable record and shows confirmation
    - User confirms file submission creates Airtable record with FileKey and shows confirmation
    - User confirms mobile grid collapses to single column
  </acceptance_criteria>
  <done>All 5 manual test scenarios pass. User has typed "approved". Submission pipeline verified end-to-end.</done>
</task>

</tasks>

<verification>
All 5 manual test scenarios pass. Airtable shows records for both file and text-only submissions.
</verification>

<success_criteria>
- File submission creates Airtable record with Name, Connection, Email, Phone, FileKey, SubmittedAt
- Text-only submission creates Airtable record with StoryText, no FileKey
- Validation blocks empty required fields and malformed email
- Confirmation screen shows personalized name
- Mobile layout stacks fields correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-form-metadata-and-confirmation/03-03-SUMMARY.md`
</output>
